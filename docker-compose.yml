version: '3.8'

services:
  frontend:
    build: 
      context: ./webapp
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    container_name: tiktok-affiliate-frontend
    restart: unless-stopped
